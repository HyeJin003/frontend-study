'use client'

// ─────────────────────────────────────────────────────────────
// TodoFooter: 카운터 + 필터 탭 + 완료 일괄 삭제
// 📖 정답: todo-100/src/app/05/components/TodoFooter.tsx
//
// TODO [Step 4-E]: 04번과 동일
//   const { activeCount, completedCount, filter } = useTodoState()
//   const dispatch = useTodoDispatch()
//   SET_FILTER / CLEAR_COMPLETED dispatch
// ─────────────────────────────────────────────────────────────

import { useTodoState, useTodoDispatch } from '../context'
import type { FilterType } from '../types'

export default function TodoFooter() {
  // 여기에 작성
  return <div />
}
