'use client'

// ─────────────────────────────────────────────────────────────
// TodoSearch: 검색어 입력 컴포넌트 (05번 신규)
// 📖 정답: todo-100/src/app/05/components/TodoSearch.tsx
//
// TODO [Step 4-B]:
//   const { searchQuery, setSearchQuery } = useTodoState()
//   input value={searchQuery} onChange={() => setSearchQuery(...)}
//   실제 필터링은 context의 useEffect 디바운스가 처리함
// ─────────────────────────────────────────────────────────────

import { useTodoState } from '../context'

export default function TodoSearch() {
  // 여기에 작성
  return <div />
}
