// ✋ 02b 안보고 치기: reducer를 직접 구현하세요
import type { Todo, TodoAction } from './types'

export function todoReducer(state: Todo[], action: TodoAction): Todo[] {
  // TODO: switch(action.type) 구현
  return state
}
